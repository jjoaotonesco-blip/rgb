name: Test MKMINIPRO Green

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  green:
    runs-on: [self-hosted, Windows, X64]
    timeout-minutes: 5

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Run MKMINIPRO green test
        shell: powershell
        run: .\scripts\test-mkminipro-green.ps1
